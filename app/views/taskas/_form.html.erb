<%= form_with(model: taska, local: true) do |form| %>
  <% if taska.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taska.errors.count, "error") %> prohibited this taska from being saved:</h2>

      <ul>
      <% taska.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
